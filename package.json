{
  "name": "jaf",
  "version": "1.0.0",
  "description": "Justice Access Foundation website",
  "scripts": {
    "format": "prettier --write --ignore-unknown \"**/*.{html,js,ts,json,css,scss,md}\"",
    "format:check": "prettier --check --ignore-unknown \"**/*.{html,js,ts,json,css,scss,md}\""
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
